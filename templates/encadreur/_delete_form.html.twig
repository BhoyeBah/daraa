<form method="post" action="{{ path('app_encadreur_delete', {'id': encadreur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encadreur.id) }}">
    <button class="btn btn-sm btn-danger"><i class="dw dw-delete-3"></i></button>
    </form>