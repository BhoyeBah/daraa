<form method="post" action="{{ path('app_dahiras_delete', {'id': dahira.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dahira.id) }}">
    <button class="btn btn-danger btn-sm"><i class="dw dw-delete-3"></i></button>
</form>